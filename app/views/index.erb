<div class="container">
  <p>
    <h1>User Sign In</h2>
    <form action='/' method="post">
      Email:    <input type="text" name="[user]email"><br><br>
      Password: <input type="password" name="[user]password">
    <input type="submit" value="Log In">
    </form>
  </p>
  <br>
  <br>
  <p>
    <a href="/new">Create a new user</a>
  </p>
</div>


<!-- when the user hit the submit button:
if the email exists, and the password matches, the user goes to the secret page.
else if the email does not exist, or if the password doesn't match, error message. -->

<!-- if submitted email in the database
  find the id of that email
elsif submitted email is not in database
  alert that email is invalid

elsif the submitted password matched the password at that index
    redirect to secret page
  else
    alert that password is invalid -->
